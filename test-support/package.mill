package build.`test-support`

import mill.*

// Not published: shared test infrastructure (Pilot driver, Buffer assertion matchers),
// consumed by other modules' test submodules only.
object `package` extends build.TuiModule {

  def moduleDeps = Seq(build.core, build.terminal, build.runtime)

  object test extends TuiTests
}
